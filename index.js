function addNum(a, b) {
  return a + b;
}

for (let index = 0; index < array.length; index++) {
  const element = array[index];
}

const myfun = async (x, y) => {
  return (await x) + y;
};
console.log(myfun(8, 8));
